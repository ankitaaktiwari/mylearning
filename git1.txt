git version - gives the version of git on your machine
git clone <repo_URL> <Directory> - this will clone the mentioned repository from remote to local
git add <filename> - this will add the changes to statging area
git commit -m "message"
git push : this will push the local changes to remote repository
git pull : this will pull the latest version of remote to local
